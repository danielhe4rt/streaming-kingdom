# Context: Application

**Layer:** `src/application/` · **Triage label:** `area:application`

Orchestration and shared state. Wires domain types to infrastructure adapters and the
presentation layer. Modules: `config`, `state`.

## Glossary

_(Stub — populated lazily by `/grill-with-docs` as terms get resolved.)_

## Decisions

See `docs/adr/` (system-wide) and `src/application/docs/adr/` (this context, when present).
